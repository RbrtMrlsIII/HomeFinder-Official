/* Removed: #admin-link-btn no longer exists on profile.html.
 * Ops consoles live in Settings (ops-staff / ops-mod / ops-admin).
 * Kept as empty module so old cached HTML script tags do not 404.
 */
export {};
