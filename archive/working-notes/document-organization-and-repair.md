# HomeFinder — Document Organization & Repair

Status: **LIVING / ADJUSTABLE**

## Active source
`active_development/` is the current repair/development basis.

## Historical source
`05_source_review/` remains a sealed historical reconstruction snapshot and is not the active source. Its old light/dark theme files and legacy model-path statements may remain for forensic recovery, but they are not active project behavior.

## Canonical documents
Active canonical documents live under `docs/`. No standalone Markdown/text authority files should be added at project root. Machine-readable contracts live under `data/`, `firebase/`, or `supabase/` according to ownership. Recovery material belongs under the recovery/reconciliation directories.

## Repair rule
During development, repairs are recorded beside the affected authority or registry. Do not create orphan notes that contradict the active SoT. Superseded documents are moved to an archive folder instead of being silently edited into historical fiction.

## Current master scene
`master/HomeFinder.sh3d` is the sole frozen architectural source; no other SH3D file participates in current authority.
