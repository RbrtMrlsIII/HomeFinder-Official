# Historical Verification Tree

This directory is retained as historical verification evidence.

Current canonical verification lives under the repository root `verify/` and current evidence lives under `docs/evidence/`.

Do not treat phase/patch audit files here as the current verification contract.
