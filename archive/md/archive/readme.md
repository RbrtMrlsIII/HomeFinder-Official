# Archive

Material placed here is historical, superseded, or retired.

Archive material is retained for provenance and regression context only. It must never be used as current authority.

When migrating old numbered/patch/DD documents, preserve their original content and record the classification in `docs/reconciliation/`.
