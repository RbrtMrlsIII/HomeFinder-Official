# 5.5G.6I — Physical Portal / Route Validation Reconnaissance

Date: 2026-08-28
Status: **BLOCKED — physical portal proof not established**

## Prior-gate verification

- 5.5G.6C protected-logic tests: PASS (8 assertions across the two active suites).
- 5.5G.6E camera reconciliation: PASS by recorded checkpoint.
- 5.5G.6F merge alignment: PASS by recorded checkpoint.
- 5.5G.6H canonical SH3D cleanup: PASS by recorded checkpoint.
- Current canonical SH3D verifier: PASS.
- Current runtime SH3D reference verifier: PASS.
- Current route/authority/storage/KYC verifiers: PASS after reconciling stale contract paths.

## Current canonical model

- `master/HomeFinder.sh3d` is the only `.sh3d` file remaining.
- Current SHA-256: `2463bbf41a92012bbd81b66ea957c993075f5a2bf6db8a43e676b0c832b0e58c`
- Raw XML counts: 7 levels, 47 rooms, 170 walls, 86 door/window elements, 15 observer cameras.

## Reconciliation improvements performed

1. Refreshed `active_development/3d/docs/model-census.json` from the current canonical `Home.xml`; the previous census contained obsolete 3-level / 13-room / 42-wall / 27-door data and an obsolete hash.
2. Corrected stale verifier references from retired `docs/contracts/...` paths to the current `docs/json/...` authority files.
3. Preserved the existing protected-logic freeze unchanged; its two hash mismatches are recorded below instead of being silently overwritten.

## Physical topology evidence

The current topology contract records these extents:

- House 1: x=-187.50002..1035.4973
- House 2: x=2800.6812096176254..4197.519027617625
- House 3: x=5107.830864040103..6248.2560640401025

Recorded x-separations are approximately 1765.184 units between House 1/2 and 910.312 units between House 2/3. This confirms that the houses are spatially distinct in the canonical coordinate system. It does **not** by itself establish the intended inter-house transport mechanism.

No runtime physical route is promoted. There is no existing validated portal binding that can safely authorize House 1↔House 2 or House 1↔House 3.

## Architectural blocker

The gate currently says **Physical Portal / Route Validation**. Before authoring runtime routes, the project owner must decide whether inter-house movement is intended to be:

1. **Physical SH3D traversal** — requiring actual portal/door evidence in the canonical physical model, or
2. **Controlled non-physical transition** — such as a map/transport transition represented separately from room traversal.

Option 2 cannot be silently substituted for the existing physical-portal gate.

## Protected freeze reconciliation

The current `5.5G.6C` protected-logic freeze verifier reports hash mismatches for:

- `docs/json/3d-contract.json`
- `docs/g1/5.5G6-house-topology-contract.json`

Both files now contain the post-promotion 5.5G.6G/6H state, including the current canonical SHA-256 and three-house topology. The mismatch therefore appears to be **freeze-version drift**, but it requires explicit checkpoint reconciliation rather than an unrecorded hash rewrite.

## Safety state

- runtime physical routes: **0**
- House 2↔House 3 direct traversal: **forbidden**
- physical travel changes role: **never**
- Firebase deployment: **not performed**
- canonical SH3D geometry: **not modified**

## Owner decision required

Resolve the intended inter-house movement mechanism before route promotion.
