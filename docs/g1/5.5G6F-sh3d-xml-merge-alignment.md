# 5.5G.6F — SH3D/XML Merge Alignment

## Result

**PASS — existing merge reconciliation found and was repaired into a merge-ready staging candidate.**

This gate does **not** replace `master/HomeFinder.sh3d` and does not promote the staging candidate to canonical authority.

## Existing reconciliation discovered

The checkpoint already contained:

- `docs/audit/phase5.5b-sh3d-merge-dry-run.md`
- `docs/md/5.5f2-deep-additional-sh3d-census.md`
- `archive/csv-reconciliation/5.5f2-merge-candidate-resource-reference-audit.csv`
- `3d/staging/HomeFinder-merge-candidate-Additional-reconciled.sh3d`

The prior work explicitly names the staging file as a deterministic merge candidate and records the same resource-reference defect.

## Structural alignment performed

The existing candidate was compared against:

- canonical `master/HomeFinder.sh3d`
- source `3d/imports/Additional.sh3d`

Confirmed:

- canonical 3-house merge had **not** yet been committed to `master/HomeFinder.sh3d`;
- canonical elements remain preserved in the candidate;
- Additional levels are represented as `add-level0` through `add-level3`;
- Additional geometry/resource payloads are present;
- Additional resource bytes in the candidate match their source payloads.

## Defect repaired

The prior candidate had malformed Additional resource references such as:

`A_1A_1A_8/wardrobe.obj`

instead of the intended namespace form:

`A_118/wardrobe.obj`

The same namespace corruption affected model/icon/image references.

The candidate has now been repaired using the source `Additional.sh3d` resource mapping:

- Additional model `N/file.obj` → `A_N/file.obj`
- Additional icon `N` → `A_N`
- Additional texture image `N` → `A_N`

No resource bytes were regenerated; only the XML references were corrected.

## Validation

- XML parses successfully: **PASS**
- candidate IDs unique: **PASS**
- canonical IDs preserved in candidate: **PASS**
- 7 levels present: **PASS**
- all model/icon/image references resolve to archive entries: **PASS**
- canonical master SH3D modified: **NO**
- runtime routes promoted: **0**
- House 2 ↔ House 3 direct connection created: **NO**

## Merge readiness

**The staging candidate is now structurally merge-ready from the previously identified XML/resource-reference defect.**

This does **not** mean the final canonical merge is automatically approved. Before replacing/updating the canonical master, the next gate must validate:

1. three-house spatial allocation;
2. level/elevation separation;
3. physical door/wall relationships;
4. camera/resource reconciliation;
5. application physical identifiers;
6. House 1-only inter-house transit rule;
7. absence of House 2 ↔ House 3 direct topology;
8. preservation of all frozen role/session logic.

Only after those validations pass should the staging candidate be promoted into the single canonical `master/HomeFinder.sh3d`.

## Important topology rule

A single SH3D authority does **not** imply a fully connected world.

The target canonical model is:

`House 2 → House 1 → House 3`

with:

`House 2 ↔ House 3 = no direct physical portal`

and physical travel never changing authenticated role.

## Gate verdict

**5.5G.6F = PASS — XML/resource alignment repaired; canonical merge intentionally not yet promoted.**
