# 5.5F.3 — Candidate Integrity & Level Reconciliation

**Execution status:** COMPLETE  
**Execution mode:** controlled authoring repair; no runtime/web code changed  
**Date:** 2026-08-26

## Objective

Validate whether `HomeFinder-merge-candidate-Additional.sh3d` is technically coherent enough to remain the preferred merge working branch, and repair deterministic integrity defects without promoting it to canonical authority.

## Findings

### Candidate identity and preservation

- Canonical `HomeFinder.sh3d` remains protected and unchanged.
- All **249 canonical element IDs** are present in the candidate.
- No duplicate element IDs were found.
- The candidate contains **509 additional IDs**, using the `add-` namespace.
- All additional level references resolve to an existing level ID.
- Additional element spatial attributes (`x`, `y`, `elevation`, `angle`, `width`, `depth`, `height`) match the staged `Additional.sh3d` source for the compared elements.
- The candidate therefore preserves the source geometry's spatial placement while introducing a distinct logical identity namespace.

### Level reconciliation

Candidate levels:

| ID | Source/house interpretation | Elevation | Status |
|---|---|---:|---|
| `level0` | House 1 / Basement | -150 | preserved |
| `level1` | House 1 / 1st floor | 112 | preserved |
| `level2` | House 1 / Roof | 374 | preserved |
| `add-level0` | Additional candidate / 1st floor | 0 | namespaced, valid |
| `add-level1` | Additional candidate / 2nd floor | 262 | namespaced, valid |
| `add-level2` | Additional candidate / Attic | 524 | namespaced, valid |
| `add-level3` | Additional candidate / Roof | 524 | namespaced, valid |

No ID collision or dangling level reference exists.

**Important:** the different elevations are not being normalized yet. That would prematurely impose a house-transfer geometry decision. They remain authoring facts to be evaluated during physical allocation and topology reconciliation.

### Resource-reference repair

The candidate had a deterministic defect: additional model/icon/image references pointed at remapped forms that did not consistently resolve to the candidate's actual resource payload names.

The repair:

1. retained the candidate's existing `A_*` resource payload namespace;
2. rewrote additional model references to the corresponding `A_<source-resource-id>/<filename>` payload;
3. rewrote additional icon/image references to `A_<source-resource-id>`;
4. reconciled additional texture image references using the source-vs-canonical texture set;
5. preserved canonical resource references untouched.

Post-repair validation:

- **0 missing model references**
- **0 missing icon references**
- **0 missing image/texture references**
- **0 duplicate element IDs**
- **0 dangling level references**

## Artifact decision

The original candidate remains as historical evidence:

`3d/staging/HomeFinder-merge-candidate-Additional.sh3d`

A repaired working candidate is added:

`3d/staging/HomeFinder-merge-candidate-Additional-F3-reconciled.sh3d`

The repaired candidate is **not canonical**.

The canonical authority remains:

`master/HomeFinder.sh3d`

Canonical SHA-256:

`0e4d75bcedbf2d9827917cd61c01780d0c1d4ba9b852dbcac65ca63e8353cb34`

## Architectural interpretation

The candidate is now technically coherent enough to serve as the preferred **merge working branch**.

It still does not prove:

- three-house room allocation;
- house-transfer door topology;
- walkable routes;
- final camera placement;
- runtime spatial contract;
- replacement of historical navigation code.

## Exit confirmation reminder

A user confirms a **logical page/presentation transition once**, before the physical journey begins. Intermediate doors are traversal mechanics, not additional confirmation points.

`source page/presentation -> confirmation -> route/doors -> destination page/presentation`

This applies equally to same-house and cross-house journeys.

## Next gate

**5.5F.4 — Three-House Candidate Physical Allocation**

Use the now-repaired candidate inventory to determine physical room/zone assignments for House 2 and House 3, while allowing many-to-many logical presentation reuse.
