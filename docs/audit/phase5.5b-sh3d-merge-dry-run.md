# Phase 5.5B — Deterministic SH3D Merge Dry Run

A temporary candidate is preserved at `3d/staging/HomeFinder-merge-candidate-Additional.sh3d`.

Rules applied:
- canonical HomeFinder geometry is preserved;
- Additional IDs are namespaced with `add-`;
- Additional numeric resource roots are namespaced with `A_`;
- Additional observer cameras are excluded from authority;
- candidate is not canonical;
- merge acceptance is blocked pending Phase 5.5C UI/CSS/JS census and House 2/3 allocation.

The candidate is a staging artifact only.
