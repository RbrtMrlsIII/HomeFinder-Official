# HomeFinder Project State

This is the canonical state entry point. Phase numbers and historical patch identifiers are not authoritative.

Current authority is defined by `PROJECT_AUTHORITY.json` and the contracts beneath `docs/json/`.

Historical phase, patch, and DD documents remain useful evidence but do not define current architecture.
