# Package structure

```
master/HomeFinder.sh3d
active_development/          # app + tests + tools
  tests/                     # all *.test.mjs
  tools/                     # CLI only
  js/ css/ data/ docs/
reconciliation/              # historical evidence
archive/checkpoints/                # audits / camera visual tuning
docs/                        # canonical + archive + DEAD_FILES list
verify/                      # legacy top-level runners
```
