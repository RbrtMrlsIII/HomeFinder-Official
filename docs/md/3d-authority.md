# 3D Authority

## Canonical master
`master/HomeFinder.sh3d`

## Status
**CANONICAL / LOCKED**

This is the only `.sh3d` file permitted in the project checkpoint.

The previous architectural predecessor and viewer-demo SH3D artifacts were compared, their relevant differences were reconciled into the canonical decision, and the redundant files were removed per explicit project authority instruction.

## Current canonical fingerprint
SHA-256: `0e4d75bcedbf2d9827917cd61c01780d0c1d4ba9b852dbcac65ca63e8353cb34`

## Camera authority
The canonical model contains nine HomeFinder cameras (`HF H-01` through `HF H-09`, full names include role suffixes e.g. `HF H-01 — hero`) with embedded presentation-only metadata and `TUNED_VISUAL_REVIEW` status. This is an implementation/evidence state, not a claim of a fresh rendered visual review in this session.

## Change rule
Any future edit requires:
1. compare the proposed state;
2. repair/reconcile;
3. verify;
4. recalculate SHA-256;
5. update evidence/contracts;
6. replace the locked state deliberately.

3D presentation never authorizes authentication, roles, routes, business data, or security.
