# 5.5C-V.2 — Current Test Path Repairs and Canonical-Role Migration — 2026-08-26

## Execution

This gate was executed from `active_development/` using Node.js v22.16.0. The purpose was to repair only objectively stale test path resolution and migrate the single current assertion that lagged behind the canonical-role implementation.

No runtime architecture, WalkMyPlan authority, or canonical SH3D geometry was restored or modified.

## Applied changes

### Test path repairs

The following tests were corrected to resolve repository artifacts from the application/repository root rather than the `tests/` directory:

- `active_development/tests/house-camera-rig.test.mjs`
  - `new URL('.', import.meta.url)` → `new URL('../', import.meta.url)`
- `active_development/tests/listings-book-interaction.test.mjs`
  - same application-root correction
- `active_development/tests/physical-ui-object-system.test.mjs`
  - same application-root correction
- `active_development/tests/market-profile-physical-ui-propagation.test.mjs`
  - application assets resolve from `active_development/`
  - repository-level reconciliation matrix resolves from `docs/csv/from-app-data/`

`patch-57-cinematic-camera-hero.test.mjs` was already correctly repaired in the cumulative checkpoint and was re-executed as part of this gate; no additional change was necessary.

### Canonical-role migration

`active_development/tests/patch-21-role-need-help.test.mjs` was migrated from the obsolete direct `data.accountType` assertion to the current canonical-role implementation:

`canonicalRoleFromData(data) === "broker" && !isOpsUid(user.uid)`

The runtime implementation was not weakened or reverted.

## Individual execution result

The six affected test files were executed individually:

| Test | Result |
|---|---|
| `patch-57-cinematic-camera-hero.test.mjs` | PASS |
| `house-camera-rig.test.mjs` | PASS |
| `listings-book-interaction.test.mjs` | PASS |
| `market-profile-physical-ui-propagation.test.mjs` | PASS |
| `physical-ui-object-system.test.mjs` | PASS |
| `patch-21-role-need-help.test.mjs` | PASS |

These represent **12 previously classified failing subtests**: 11 path-related subtests plus the single Patch 21 contract-drift assertion.

## Active-suite result

The active suite was rerun using the same top-level `tests/*.mjs` execution scope used for the 95-subtest classification baseline.

- **95 subtests**
- **61 passed**
- **34 failed**

The reduction from 42 to 34 failures is exactly the expected **8 newly repaired subtests**. The four Patch 57 subtests were already passing in the cumulative checkpoint and therefore did not change the count.

The remaining 34 failures are not automatically product defects. They belong to the previously classified historical/superseded documentation-contract group and must be handled by the next migration/archive gate.

## Authority integrity

- Active WalkMyPlan test references: **0**
- `master/HomeFinder.sh3d`: unchanged
- Canonical SH3D SHA-256: `0e4d75bcedbf2d9827917cd61c01780d0c1d4ba9b852dbcac65ca63e8353cb34`

## Architectural interpretation

This gate confirms that the current repository layout and canonical-role contract can support the affected tests without restoring historical architecture.

The important distinction remains:

`test path correction` is test maintenance, not product architecture.

`canonicalRoleFromData` migration is validation alignment, not runtime regression.

Neither operation authorizes a physical 3D merge.

## Next gate

**5.5C-V.3 — Historical-contract test reconciliation**

Take the remaining 30 previously classified patch-era documentation/governance failures and inspect their runtime assertions individually.

For each:

1. identify behavior still required by current application contracts;
2. migrate only those assertions to current authority;
3. archive tests whose entire contract is superseded;
4. run each migrated test;
5. record the resulting current validation baseline.

Only after the validation branch has a trustworthy current-contract baseline should it rejoin the master chronology at three-house allocation / UI ↔ 3D presentation-contract reconciliation.
