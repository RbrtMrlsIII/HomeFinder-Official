# 5.5C-V.6 — Baseline Integrity / Rejoin Audit

**Date:** 2026-08-26  
**Status:** COMPLETE — rejoin authorized with explicit spatial prerequisites

## Purpose

This gate verifies that the cumulative V5 checkpoint is internally consistent before leaving the validation-reconciliation branch and returning to the master 3D chronology.

## Execution

The cumulative V5 checkpoint was extracted and the active test suite was rerun from the required `active_development/` working directory:

```text
node --test tests/*.test.mjs
```

Result:

- 66 active `.mjs` test modules are present in the active test tree, with additional `.js` tests included by the globbed test run.
- 115 subtests
- 97 passed
- 18 failed

This **reproduces** the previously reported V4 115/97/18 result. Therefore the V5 statement that 92/92 was the authoritative current baseline was incorrect for this cumulative checkpoint. V5's 92/92 result is retained as historical execution evidence, but is superseded by this reproducible V6 run.

## Failure population

The 18 failures are concentrated in four classes:

1. **Retired/missing dictionary contract paths**
   - Patch 37/39/41/42 authority tests
   - Patch 35 dictionary foundation
   - These reference `active_development/docs/dictionary/...` paths that are absent.

2. **Stale `tests/`-relative paths into active application assets**
   - animation/object responsive root
   - data vocabulary
   - design roots
   - Main Hall camera/object contract
   - Main Hall physical UI roots
   - master matrix
   - Phase 11 deploy readiness
   - public-profile migration/read surface
   - These tests resolve paths beneath `active_development/tests/` instead of the current active application root.

3. **Historical Market documentation contract**
   - Market Patch 07/08/09
   - These still require retired `docs/core/01-SOURCE-OF-TRUTH.md`.

4. **Historical Patch 15 listing activity contract**
   - `patch-15-listing-activity.test.js`
   - Requires separate reconciliation against the current listing-activity contracts.

These failures were **not repaired during V6**. They remain the next controlled reconciliation population. No runtime implementation was changed to manufacture a green baseline.

## Authority verification

`docs/json/project-authority.json` resolves all canonical authority references successfully.

Canonical 3D authority:

`master/HomeFinder.sh3d`

SHA-256:

`0e4d75bcedbf2d9827917cd61c01780d0c1d4ba9b852dbcac65ca63e8353cb34`

No WalkMyPlan test references are active.

## Manifest integrity

The previous `active_development/CHECKPOINT_MANIFEST.json` contained a stale Phase 13 file inventory and stale hashes. It was regenerated from the cumulative checkpoint during this gate and now records the actual active-development inventory and hashes.

`docs/json/checkpoint-manifest-root.json` remains a historical/root checkpoint manifest and contains an older SH3D hash. It is retained as evidence rather than silently rewritten.

The model census also contains a historical source-model hash that does not equal the current canonical SH3D hash. This is recorded as a census-evidence reconciliation item; the census geometry counts are not promoted to current authority until verified against the canonical model.

## Spatial readiness finding

The existing census is **not empty and must not be restarted**:

- logical camera reconciliation: 42 entries, all `REBUILD_REQUIRED`
- logical room reconciliation: 9 entries, all `VIRTUAL_APP_ROOM_REQUIRES_MAPPING`
- current model census: 13 room polygons and 6 observer cameras

Therefore the project may **rejoin the master chronology**, but the next stage is not a blind SH3D merge. Three-house allocation and the UI ↔ 3D presentation contract must first reconcile the existing logical census against the physical model.

## Decision

**5.5C-V.6 COMPLETE.**

The validation-reconciliation branch is closed as a controlled branch. It is safe to rejoin the master chronology with the following next gate:

**Three-house allocation + UI ↔ 3D presentation-contract reconciliation**

The 18 active-suite failures remain a separate validation-debt population and must be reconciled without restoring obsolete authority.

