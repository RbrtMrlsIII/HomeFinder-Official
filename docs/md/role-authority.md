# Role Authority

Canonical roles:
- owner
- seeker
- broker
- staff
- moderator
- admin

`canonicalRole` is the single current role authority.

`accountType`, legacy `role`, and `agent` may be normalized at boundaries where required for compatibility, but none may contradict or override `canonicalRole`.
