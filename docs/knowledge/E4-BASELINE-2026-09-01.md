# HomeFinder E4 — Knowledge & Anti-Repeat Baseline

Date: 2026-09-01
Milestone: E4
Status: EXECUTED / VALIDATED / PENDING FINAL CONTINUITY CLOSE

## Existing authoritative knowledge

`PRODUCT-KNOWLEDGE.md` remains the canonical durable project-knowledge authority. It already contains extensive engineering principles covering source-vs-derived authority, dependency-aware architecture, verification classification, read-only automation, idempotence, provenance, risk/assumption handling, progressive confidence, engineering honesty, and the final non-negotiable rules.

`project-guide/repository-governance/FINDINGS-TO-KNOWLEDGE.md` remains the lifecycle owner for finding → verified knowledge → continuity → retention/archive/delete decisions.

## E4 gap resolved

The missing execution capability was machine-discoverable anti-repeat checking before Classify.

Added:

- `.agent/knowledge/ANTI-REPEAT-INDEX.json` — derived trigger/rule/source index;
- `scripts/knowledge-search.py` — deterministic search helper;
- `project-guide/repository-governance/KNOWLEDGE-AND-ANTI-REPEAT.md` — E4 procedure and acceptance boundary.

No second Product Knowledge authority was created.

## Indexed anti-pattern classes

The initial index records 11 high-value lessons:

1. old P04 Three.js module-resolution hypothesis;
2. P04 repository-backed GLB availability blocker;
3. superseded WalkMyPlan runtime/spatial architecture;
4. replacing SH3D authority with derived artifacts;
5. HTTP 200/static availability mistaken for application success;
6. weakening acceptance assertions to make CI green;
7. inventing spatial geometry/coordinates;
8. blanket project-document updates;
9. fabricated full-project counts from truncated remote API views;
10. duplicate semantic dictionary creation;
11. Git history rewrite without a dedicated reconciliation gate.

Each indexed rule points to an existing canonical source rather than copying full reasoning into the machine index.

## Validation

The helper was syntax-checked and behavior-checked against a representative index fixture:

- matching anti-pattern query returns exit code 2;
- unrelated query returns exit code 0;
- `--score` returns deterministic match scores.

The live repository index and the canonical documents remain the sources of truth; the index is a derived navigation layer.

## Boundary

E4 does not yet enforce the anti-repeat search in CI/pre-commit. Automated enforcement is E7. E4 establishes the machine-readable capability and the required execution behavior.
