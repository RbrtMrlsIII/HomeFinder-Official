# E7 validation trigger

This file intentionally exists to trigger the path-scoped E7 governance workflow after the enforcement workflow implementation was corrected.

It contains no product/runtime instruction and is not an authority source.
