# P04.3–P04.6 Spatial / Visual Validation Findings — 2026-09-01

## Classification
P01–P03 and P04.0–P04.2 are accepted historical product gates. The first unchecked P gate is P04.3.

## Findings

1. The source-backed P04 target manifest defines five validation targets and preserves source-bound camera contracts.
2. The final whole-project E checkpoint contains the exact four approved GLB binaries. Static SHA-256 and GLB v2 header/length checks passed 4/4.
3. The live P-series branch does not contain the canonical four binary files at the manifest runtime paths. A historical/base64 workaround exists on an older P04 line and is not promoted.
4. The P04 viewer is separate from canonical SH3D authority. No SH3D mutation is required for the current gate.
5. A real Chromium/Playwright P04 workflow has been added using the proven browser execution shape. It requires the exact approved binaries at the canonical repository paths before browser assertions run.
6. GitHub run `33480279339` executed the new P04 workflow and failed at the repository-backed GLB availability gate. This is an expected bounded failure: browser assertions were not run against substitute assets.
7. Local dependency installation also timed out in the execution environment and the external Three.js/GLTFLoader modules are not available offline. No local browser pass is claimed.
8. Therefore P04.3, P04.4 and P04.5 remain not endorsed, and P04.6 is held.
9. Repository search did not reveal an authoritative P05 or P06 acceptance specification. Advancing those gates now would invent product requirements, so P05 and P06 remain held.

## Decision

P-series execution is in **P04 validation hold**. This is a truthful evidence boundary, not a fabricated product verdict.

No placeholder geometry, mocked renderer, weakened assertion, alternate SH3D authority, or unverified GLB promotion was used.

### Enabling condition

Promote the four exact approved GLB binaries into the canonical repository paths in a binary-safe way, then rerun the existing Chromium workflow. Only after fresh target/camera/scale/elevation/GLB-load evidence passes may P04.3–P04.5 be endorsed and P04.6 closed.
